@using Entities;
@model IEnumerable<VCardModel>

@{
    ViewData["Title"] = "Home Page";
}



<div class="container my-4">
      <form asp-action="Index">
        <div class="input-group mb-3">
            <input type="text" class="form-control" name="count" placeholder="Enter contact count">
            <button class="btn btn-outline-secondary" type="submit"  id="button-add-vcards">
                Get Contact
             </button>
        </div>
     </form>

    @*@Html.Partial("_ContactTable",@Model)*@
    @Html.Partial("_ContactCards",@Model)
</div>
